[Unit]
Description=Pomodoro timer
Requires=docker.service
After=docker.service

[Service]
WorkingDirectory=/opt/my/pomodoro-timer
ExecStart=/usr/bin/docker compose up --quiet-pull
ExecStop=/usr/bin/docker compose down
Restart=always
TimeoutStartSec=60

[Install]
WantedBy=multi-user.target